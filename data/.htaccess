# 禁止通过 Web 直接访问 data 目录（含历史 JSON、install.lock 等）
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>

Options -Indexes
